import Input from "../../../subComponents/Input.js";
import { DEPARTURE_STATION_INPUT_ID } from "../../../../common/DOMelementNames.js";

export default new Input({ id: DEPARTURE_STATION_INPUT_ID });
